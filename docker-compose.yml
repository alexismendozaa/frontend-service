version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    depends_on:
      - db
    command: ["node", "src/server.js"]

  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"    # Nginx para producción
      - "5173:5173"  # Mapea el puerto 5173 para desarrollo de Vite
    depends_on:
      - backend
    command: ["npm", "run", "dev"]  # Ejecutar Vite en modo desarrollo

  db:
    image: postgres:14
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: alexis
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: {}
